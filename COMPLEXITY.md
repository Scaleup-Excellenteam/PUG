# ניתוח סיבוכיות וביצועים

נסמן:

- `S` — מספר המשפטים (השורות הלא־ריקות).
- `N` — מספר התווים הכולל לאחר נרמול.
- `L_i` — אורך המשפט ה־`i`.
- `m` — אורך השאילתה המנורמלת.
- `b` — מספר הענפים האפשריים בצומת Trie.
- `C` — מספר המועמדים הייחודיים שנאספו לפני בחירת החמישה.

## ה־Trie הדחוס

בנייה: לכל משפט מוכנסים כל `L_i` הסיומות המתחילות בכל תו. מאחר שסך אורכי
הסיומות הוא `L_i(L_i+1)/2`, זמן הבנייה במקרה הגרוע הוא
`O(sum(L_i^2))`. דחיסת מסלולים מורידה מאוד את מספר אובייקטי Python, אך אינה
משנה את מספר התווים שיש להשוות בזמן ההכנסה. מספר העלים והצמתים המפורשים הוא
`O(N)`, ובכל צומת נשמרים לכל היותר 20 מזהים. מאחר שתוויות הקשתות הן מחרוזות
Python נפרדות, סך זיכרון התוויות עלול להגיע למקרה גרוע ריבועי.

חיפוש מדויק עובר לאורך מסלול השאילתה ולכן עולה `O(m)` ועוד קריאה של עד 20
מזהים. בחיפוש עם תקציב עריכה אחד ה־DFS שומר את המצב
`(cursor, query_index, edit_used)` ומונע עיבוד חוזר של מצב עם ניקוד נמוך יותר.
הגבול הגרוע הוא `O(b*m^2 + C log C)`: ניתן לבצע את העריכה בכל מיקום, לנסות
ענפים חלופיים, ולהמשיך להתאים את יתר השאילתה. בפועל, דחיסת הקשתות ותקציב
העריכה היחיד מקטינים מאוד את מספר המצבים.

## מנוע SQLite המדרג מראש

בשלב ה־offline הנרמול והכנסת התוכן הם `O(N)`. בניית שני סדרי הדירוג דורשת שני
מיונים של `S` רשומות, כלומר `O(S log S)`, ובניית אינדקסי trigram היא ליניארית
בקירוב במספר ה־trigram-ים (`O(N)`). מטמוני תו וביגרם נשמרים בגודל קבוע 20.

בחיפוש מדויק באורך שלושה תווים ומעלה מתבצעת גישת FTS אחת שעוצרת אחרי 20
תוצאות שכבר סודרו offline. העלות המעשית היא בקירוב `O(m + 20)`. אם נמצאו חמש
התאמות מדויקות במצב המטלה, הן בהכרח גוברות על כל התאמה עם שגיאה והחיפוש נעצר.

לשאילתה ארוכה ללא חמש התאמות מדויקות נוצרים `O(m)` מסלולי עריכה. כל מסלול
משתמש בחלקים הקבועים שמשני צדי התו כעוגני trigram ומאמת תבנית בעלת תו יחיד.
לכן אין מכפלה בגודל האלפבית. העלות היא `O(m*P + C log C)`, כאשר `P` הוא מספר
הרשומות ברשימות העוגן שנבדקו; עוגנים ארוכים הופכים את `P` לקטן בדרך כלל.
לשאילתות קצרות, שבהן אין עוגן trigram בטוח, נוצרים וריאנטים בעלות
`O(|alphabet|*m)` ונעשה שימוש גם במטמוני תו/ביגרם.

## מדידה על `Archive/Archive.zip`

- 2,583,991 משפטים הוכנסו לאינדקס.
- זמן בנייה מלא: 195.1 שניות.
- `sentences.pkl`: 300.73 MiB.
- `sentences.sqlite3`: 1,135.51 MiB.
- טעינה שנמדדה: 3.01 שניות עם cache מערכת חם; בהרצות קרות נצפו גם כ־11 שניות.
- `this is`: חציון חם 0.159 ms.
- `the quick brown`: חציון חם 4.767 ms.
- `or knot` (שגיאת תו אחת): חציון חם 24.379 ms.
- `python algorithm` (התאמה נדירה עם שגיאה): חציון חם 290.610 ms.

המספרים תלויים ב־cache של מערכת ההפעלה, בדיסק ובשכיחות העוגנים בקורפוס. אפשר
לשחזר את המדידה באמצעות `python benchmark.py`.
